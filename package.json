{
  "name": "eco-log",
  "version": "1.0.0",
  "description": "Eco Log - painel integrado (Electron)",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . EcoLog --platform=win32 --arch=x64 --out=dist --overwrite --icon=assets/icon.ico"
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "electron-packager": "^17.1.0"
  }
}